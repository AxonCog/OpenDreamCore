# 更新日志

新版本在上，旧版本在下。格式：`版本号（日期）`，下面按模块说事。

## v0.1.1（2026-08-25）

这版主要是把 DreamCore 的东西往 OpenDreamCore 迁，大头都迁完了，捡重点说。

### 版本铺开

从 1.20.1 到 26.1.2 全线铺开，Forge / Fabric / NeoForge 三个加载器、12 个 target 全部编译出包，共享源码一行没改。架构就摆在这，以后跟进新的小版本，我们一小时内就能完成适配。

### 适配器

适配器整套从 DreamCore 迁了过来。演示适配器直接兼容梦想核心语法——还在用龙核的服务器不用改任何东西，换到 OpenDreamCore 开箱即用。

龙核那套脚本方法也一并迁齐：变量、字符串、数组、数学、聊天、音效调度这些常用的全都能对上。

写适配器很简单，实现几个方法就能接入一种新语法，照着演示写一个不难。萌芽之类的兼容后面随时可以加，也欢迎有兴趣的人自己动手。

### UI

UI 完整迁移过来了。之前迁过一轮但有部分遗漏，这版全部补齐——从这版开始 UI 是完整的，可以放开手全面开发了。

- 脚本方法注册表按域拆分（迁移时顺手整理的）：Screen / Player / Var / Sound / Camera / Key / Mouse……各归各类，找起来不用翻一个上千行的大文件了
- 世界面板可视化编辑器连着撤销/重做一起迁了过来：Ctrl+Z 撤、Ctrl+Y 重做，上限 64 步，拖拽落位、属性修改、增删元素全都可撤
- 顺手修了些 bug

### 窗口标题接管

这套也是从 DreamCore 原样迁过来的：进服自动接管客户端窗口标题，多句轮播、打字机逐字效果都有，断线或指令复位自动还原本地 branding。服务端 `config.yml` 的 `client-title` 段配置，原版客户端自动忽略、不受影响。

配套的 TitleAPI 一并迁入，附属插件想接管/还原玩家窗口标题，调接口就行。

### 材质包

自定义材质包整套迁移完成。

- 本地预置：启动自动扫描 resourcepacks/OpenDreamCore/，文件或文件夹扔进去就置顶加载，不用服务器下发，中文文件名也没问题
- 托管目录本身就能当材质包用，assets/<命名空间>/... 放进去直接生效
- PackAPI 也跟着迁过来了：服务端一条指令给玩家下发材质包安装命令，https 直链或服务器本地路径都行，支持加密 zip 密码，可置顶覆盖，后台下载解压自动生效

迁移的内容不止上面这些，剩下的自己进游戏探索。

### 工程与测试

- 加了 build-all.bat / build-all.sh 构建脚本，12 个 target 全量出包不用挨个敲命令
- fabric-1.20.1 原来散装的客户端代码收敛进共享源码树（Compat 兼容层），不再两边各维护一份
- 补了一批单元测试：适配器路由、龙核解析金样本、表达式三元运算、窗口标题推送、打字机序列、布局别名……

---

我一个人测不可能全覆盖，碰到问题直接去 GitHub 提 issue，或者群里找我，立马解决。

下一版（0.1.2）会把低版本 1.6.4–1.16.5 也迁完。

## v0.1.0（2026-08-21）

初始提交。
